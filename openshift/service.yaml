apiVersion: v1
kind: Service
metadata:
  name: customer-jira-tracker-mcp-service
  labels:
    app: customer-jira-tracker-mcp
spec:
  selector:
    app: customer-jira-tracker-mcp
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
